
export class Resta {
   x: number;
   y: number;

   constructor(x: number, y: number){
       this.x = x;
       this.y = y;
   }

   operacion () {
       return this.x - this.y;
   }
}

